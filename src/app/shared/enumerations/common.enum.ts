export enum DateSeparator {
  DOT = '.',
  HYPHEN = '-',
  SLASH = '/',
  SPACE = ' '
}

export enum MaskKey {
  ASTERISK = '*',
  CROSS = 'X'
}

export enum PhoneNumberSeparator {
  DOT = '.',
  HYPHEN = '-',
  SPACE = ' '
}

export enum FeeType {
  FREE = 'Free',
  PAID = 'Paid'
}

export enum Notify {
  DEFAULT = 'default',
  ERROR = 'error',
  INFO = 'info',
  SUCCESS = 'success',
  WARNING = 'warning',
}
